import 'package:flutter/material.dart';

class RepositoriesManagementPage extends StatelessWidget {
  const RepositoriesManagementPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('data');
  }
}
